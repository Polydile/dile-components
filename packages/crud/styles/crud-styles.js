import { css } from 'lit';

export const crudStyles = css`
h1 {
  font-size: var(--dile-crud-h1-font-size, 1.5rem);
  font-weight: var(--dile-crud-h1-font-weight, bold);
  margin: var(--dile-crud-h1-margin, 0 0 1rem 0);
}
`;